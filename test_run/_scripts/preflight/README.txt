用途：存放任务执行前的只读检查脚本。

例如：校验配置、检查连通性、采集基线、验证认证状态，以及确认目标为测试环境。

Purpose: read-only readiness checks before a task runs.

Examples: validate configuration, test connectivity, capture a baseline,
verify authentication state, or confirm the target is a test environment.
