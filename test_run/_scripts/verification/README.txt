用途：存放独立验证任务结果的脚本。

例如：API 回读断言、结果比对、报告聚合和证据完整性检查。

Purpose: scripts that independently verify task outcomes.

Examples: API read-back assertions, result comparisons, report aggregation,
and evidence integrity checks.
